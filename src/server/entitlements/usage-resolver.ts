import"server-only";import type{SupabaseClient}from"@supabase/supabase-js";import type{EntitlementFeature}from"./entitlement-catalog";
export async function resolveFeatureUsage(database:SupabaseClient,workspaceId:string,feature:EntitlementFeature){const count=async(table:string,filters?:{status?:string[];createdAfter?:string})=>{let query=database.from(table).select("id",{count:"exact",head:true}).eq("workspace_id",workspaceId);if(filters?.status)query=query.in("status",filters.status);if(filters?.createdAfter)query=query.gte("created_at",filters.createdAfter);const{count:total,error}=await query;if(error)throw error;return total||0;};switch(feature){case"projects":return count("projects");case"team_members":return count("workspace_members");case"active_activations":return count("conversion_activations",{status:["active","scheduled"]});case"presence_pages":{const{data:projects}=await database.from("projects").select("id").eq("workspace_id",workspaceId);const ids=(projects||[]).map(p=>p.id);if(!ids.length)return 0;const{count:total,error}=await database.from("presence_pages").select("id",{count:"exact",head:true}).in("project_id",ids);if(error)throw error;return total||0;}case"ai_generations_month":{const start=new Date();start.setUTCDate(1);start.setUTCHours(0,0,0,0);return count("ai_generation_runs",{createdAfter:start.toISOString()});}default:return 0;}}
