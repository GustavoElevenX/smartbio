import "server-only";
import { syncProjectRows } from "@/server/commercial-data/repository-utils";
import type { ReservationBlock, ReservableUnit } from "@/types";
export async function saveReservationConfig(projectId: string, units: ReservableUnit[], blocks: ReservationBlock[]) { await syncProjectRows("reservable_units", projectId, units.map((item) => ({ id: item.id, project_id: projectId, name: item.name, description: item.description || null, capacity_adults: item.capacityAdults, capacity_children: item.capacityChildren, quantity: item.quantity, base_price: item.basePrice ?? null, currency: item.currency, is_active: item.isActive, media_asset_ids: item.mediaAssetIds, amenities: item.amenities, settings: { depositAmount: item.depositAmount, confirmationMode: item.confirmationMode, rules: item.rules } }))); await syncProjectRows("reservation_blocks", projectId, blocks.map((item) => ({ id: item.id, project_id: projectId, unit_id: item.unitId || null, starts_on: item.startsOn, ends_on: item.endsOn, quantity: item.quantity, reason: item.reason || null }))); }
