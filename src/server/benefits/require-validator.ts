import"server-only";import{cookies}from"next/headers";import{createServiceClient}from"@/lib/supabase/server";import{readValidatorCookie}from"./validator-auth";export const VALIDATOR_COOKIE="virou_validator";export async function requireValidator(){const database=createServiceClient();if(!database)throw new Error("database_required");const id=readValidatorCookie((await cookies()).get(VALIDATOR_COOKIE)?.value);if(!id)throw new Error("validator_unauthorized");const{data}=await database.from("redemption_validators").select("*").eq("id",id).eq("is_active",true).maybeSingle();if(!data)throw new Error("validator_revoked");return{database,validator:data};}
