import { ArrowRight, GitBranch, MapPin, MessageCircle } from "lucide-react";
import type { Project } from "@/types";
export function RoutingFlow({ project }: { project: Project }) { const rules = project.commercialConfig?.routingRules || []; const destinations = project.commercialConfig?.routingDestinations || []; return <div className="grid gap-2 sm:grid-cols-[1fr_auto_1fr_auto_1fr] sm:items-center"><div className="rounded-[16px] bg-[#f5f4f8] p-4"><GitBranch size={16} /><strong className="mt-4 block text-sm">{rules.length} regras</strong></div><ArrowRight className="mx-auto rotate-90 sm:rotate-0" size={15} /><div className="rounded-[16px] bg-[#f5f4f8] p-4"><MapPin size={16} /><strong className="mt-4 block text-sm">Resolver destino</strong></div><ArrowRight className="mx-auto rotate-90 sm:rotate-0" size={15} /><div className="rounded-[16px] bg-[#f5f4f8] p-4"><MessageCircle size={16} /><strong className="mt-4 block text-sm">{destinations.length} destinos</strong></div></div>; }
