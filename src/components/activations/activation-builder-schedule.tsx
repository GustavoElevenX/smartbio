import type{ActivationBuilderState}from"./activation-builder-types";export function ActivationBuilderSchedule({state,onChange}:{state:ActivationBuilderState;onChange:(patch:Partial<ActivationBuilderState>)=>void}){return<div className="grid gap-5 sm:grid-cols-2"><label><span className="text-sm font-bold">Início</span><input type="datetime-local" value={state.startsAt} onChange={(e)=>onChange({startsAt:e.target.value})} className="mt-2 min-h-12 w-full rounded-xl border border-[#dcdbe3] px-4"/></label><label><span className="text-sm font-bold">Fim</span><input type="datetime-local" value={state.endsAt} onChange={(e)=>onChange({endsAt:e.target.value})} className="mt-2 min-h-12 w-full rounded-xl border border-[#dcdbe3] px-4"/></label><p className="text-sm text-[#707079] sm:col-span-2">Timezone: America/Sao_Paulo. A visibilidade usa a hora do servidor.</p></div>}
