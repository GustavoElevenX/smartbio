export interface ClaimRow{code:string;phoneMasked?:string;status:string;issuedAt:string}export function ActivationClaims({claims}:{claims:ClaimRow[]}){return<div className="overflow-hidden rounded-2xl border border-[#e4e3ea] bg-white">{claims.length?claims.map((claim)=><div key={claim.code} className="grid grid-cols-[1fr_auto] gap-3 border-b border-[#ecebf0] p-4 last:border-0"><div><strong className="font-mono tracking-[.12em]">{claim.code}</strong><p className="mt-1 text-sm text-[#777780]">{claim.phoneMasked||"Identidade protegida"}</p></div><div className="text-right"><span className="text-sm font-bold">{claim.status}</span><p className="mt-1 text-xs text-[#85858e]">{new Intl.DateTimeFormat("pt-BR",{dateStyle:"short",timeStyle:"short"}).format(new Date(claim.issuedAt))}</p></div></div>):<p className="p-8 text-center text-[#777780]">Nenhum claim emitido.</p>}</div>}
