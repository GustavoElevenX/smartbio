import Link from"next/link";import{ArrowUpRight}from"lucide-react";import type{ConversionActivation}from"@/features/activations/activation.types";import{ActivationStatus}from"./activation-status";export function ActivationCard({activation}:{activation:ConversionActivation}){return<article className="rounded-2xl border border-[#e5e4ec] bg-white p-5"><div className="flex items-start justify-between gap-4"><div><h3 className="font-extrabold">{activation.name}</h3><p className="mt-1 text-sm text-[#777780]">{activation.title||activation.description||"Sem descrição"}</p></div><ActivationStatus status={activation.status}/></div><dl className="mt-5 grid grid-cols-2 gap-3 text-sm"><div><dt className="text-[#85858e]">Objetivo</dt><dd className="mt-1 font-bold">{activation.conversionGoalId?"Meta conectada":"Definir"}</dd></div><div><dt className="text-[#85858e]">Validade</dt><dd className="mt-1 font-bold">{activation.endsAt?new Intl.DateTimeFormat("pt-BR",{dateStyle:"medium"}).format(new Date(activation.endsAt)):"Sem término"}</dd></div></dl><Link href={`/app/projects/${activation.projectId}/activations/${activation.id}`} className="focus-ring mt-5 inline-flex min-h-11 items-center gap-2 rounded-xl border border-[#dddce5] px-4 text-sm font-bold">Ver ativação<ArrowUpRight size={16}/></Link></article>}
