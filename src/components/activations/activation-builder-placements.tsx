import type{ActivationBuilderState}from"./activation-builder-types";const placements=[['announcement_bar','Barra superior'],['hero_override','Hero'],['conversion_cta','CTA'],['product_badge','Badge de produto'],['journey_banner','Banner da jornada'],['floating_cta','CTA flutuante']]as const;export function ActivationBuilderPlacements({state,onChange}:{state:ActivationBuilderState;onChange:(patch:Partial<ActivationBuilderState>)=>void}){return<div className="grid gap-3 sm:grid-cols-2">{placements.map(([value,label])=><label key={value} className="flex min-h-12 items-center gap-3 rounded-xl border border-[#e1e0e7] bg-white px-4 font-bold"><input type="checkbox" checked={state.placements.includes(value)} onChange={(e)=>onChange({placements:e.target.checked?[...state.placements,value]:state.placements.filter((item)=>item!==value)})}/>{label}</label>)}</div>}
