import type{ActivationBuilderState}from"./activation-builder-types";export function ActivationBuilderConversion({state,onChange}:{state:ActivationBuilderState;onChange:(patch:Partial<ActivationBuilderState>)=>void}){return<div className="space-y-3">{([['redemption_marks_conversion','Resgate confirma a conversão'],['manual_conversion','Conversão manual'],['external_conversion','Conversão externa']]as const).map(([value,label])=><label key={value} className="flex min-h-12 items-center gap-3 rounded-xl border border-[#e1e0e7] bg-white px-4 font-bold"><input type="radio" checked={state.conversionPolicy===value} onChange={()=>onChange({conversionPolicy:value})}/>{label}</label>)}</div>}
