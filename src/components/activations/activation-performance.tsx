import type{ActivationPerformance as Performance}from"@/features/activations/activation.types";const metric=(value:number)=>new Intl.NumberFormat("pt-BR").format(value);export function ActivationPerformance({performance}:{performance:Performance}){const steps=[['Visualizações',performance.views],['Inícios',performance.ctaClicks],['Identificados',performance.identities],['Claims',performance.claimsIssued],['Apresentados',performance.claimsPresented],['Resgates',performance.redemptions],['Conversões',performance.conversions]]as const;return<section><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{[...steps,["Valor confirmado",new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"}).format(performance.confirmedValue)]as const].map(([label,value])=><div key={label} className="rounded-2xl border border-[#e4e3ea] bg-white p-4"><p className="text-xs font-bold text-[#767680]">{label}</p><strong className="mt-3 block text-2xl">{typeof value==='number'?metric(value):value}</strong></div>)}</div><div className="mt-5 rounded-2xl border border-[#e4e3ea] bg-white p-5"><h3 className="font-extrabold">Funil da ativação</h3><div className="mt-5 grid gap-2 lg:grid-cols-7">{steps.map(([label,value],index)=><div key={label} className="relative rounded-xl bg-gradient-to-br from-[#f0edff] to-[#faf9ff] p-4 lg:min-h-28"><span className="text-xs font-bold text-[#6554d4]">{index+1}</span><strong className="mt-3 block text-xl">{metric(value)}</strong><span className="mt-1 block text-xs text-[#6f6f78]">{label}</span></div>)}</div><div className="mt-5 grid gap-3 text-sm sm:grid-cols-2 lg:grid-cols-4">{[['View → claim',performance.rates.viewToClaim],['Claim → resgate',performance.rates.claimToRedemption],['View → oportunidade',performance.rates.viewToOpportunity],['Oportunidade → conversão',performance.rates.opportunityToConversion]].map(([label,value])=><div key={label as string}><span className="text-[#777780]">{label}</span><strong className="ml-2">{value==null?'—':`${value}%`}</strong></div>)}</div></div></section>}
