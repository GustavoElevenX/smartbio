export interface RedemptionRow{code:string;location?:string;subtotal?:number;finalAmount?:number;redeemedAt:string}export function ActivationRedemptions({redemptions}:{redemptions:RedemptionRow[]}){return<div className="overflow-hidden rounded-2xl border border-[#e4e3ea] bg-white">{redemptions.length?redemptions.map((item)=><div key={`${item.code}-${item.redeemedAt}`} className="grid gap-2 border-b border-[#ecebf0] p-4 last:border-0 sm:grid-cols-[1fr_auto_auto]"><strong className="font-mono tracking-[.12em]">{item.code}</strong><span>{item.location||"Sem unidade"}</span><div className="sm:text-right"><strong>{item.subtotal!=null&&item.finalAmount!=null?`${item.subtotal.toLocaleString('pt-BR',{style:'currency',currency:'BRL'})} → ${item.finalAmount.toLocaleString('pt-BR',{style:'currency',currency:'BRL'})}`:'Benefício não monetário'}</strong><p className="text-xs text-[#85858e]">{new Intl.DateTimeFormat("pt-BR",{dateStyle:"short",timeStyle:"short"}).format(new Date(item.redeemedAt))}</p></div></div>):<p className="p-8 text-center text-[#777780]">Nenhum resgate confirmado.</p>}</div>}
