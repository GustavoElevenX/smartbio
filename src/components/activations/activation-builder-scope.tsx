import type{ActivationBuilderState,BuilderOption}from"./activation-builder-types";export function ActivationBuilderScope({state,locations,onChange}:{state:ActivationBuilderState;locations:BuilderOption[];onChange:(patch:Partial<ActivationBuilderState>)=>void}){return<div><p className="font-bold">Unidades participantes</p><div className="mt-3 space-y-2">{locations.length?locations.map((location)=><label key={location.id} className="flex min-h-11 items-center gap-3 rounded-xl border border-[#e2e1e8] bg-white px-4"><input type="checkbox" checked={state.locationIds.includes(location.id)} onChange={(e)=>onChange({locationIds:e.target.checked?[...state.locationIds,location.id]:state.locationIds.filter((id)=>id!==location.id)})}/>{location.name}</label>):<p className="rounded-xl bg-[#f2f1f6] p-4 text-sm text-[#74747e]">Nenhuma unidade cadastrada. A ativação valerá para todo o negócio.</p>}</div></div>}
