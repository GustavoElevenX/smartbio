import type{ActivationBuilderState,BuilderOption}from"./activation-builder-types";
export function ActivationBuilderDestination({state,destinations=[],onChange}:{state:ActivationBuilderState;destinations?:BuilderOption[];onChange:(patch:Partial<ActivationBuilderState>)=>void}){return<div><label className="block"><span className="text-sm font-bold">Canal de conclusão</span><select value={state.completionChannel} onChange={e=>onChange({completionChannel:e.target.value as ActivationBuilderState["completionChannel"],destinationMode:e.target.value==="native"?"native":state.destinationMode})} className="mt-2 min-h-12 w-full rounded-xl border border-[#dcdbe3] bg-white px-4"><option value="whatsapp">WhatsApp humano</option><option value="native">Ação nativa</option><option value="external_url">Link externo</option><option value="email">E-mail</option><option value="phone">Telefone</option></select></label>{state.completionChannel==="whatsapp"?<fieldset className="mt-6"><legend className="text-sm font-bold">Como escolher quem recebe?</legend><div className="mt-3 space-y-2">{[["routing","Usar roteamento da Virou"],["fixed_destination","Enviar sempre para um destino"],["goal_default","Usar destino configurado no objetivo"]].map(([value,label])=><label key={value} className="flex min-h-11 items-center gap-3 rounded-xl border border-[#dedce6] px-4"><input type="radio" name="destination-mode" checked={state.destinationMode===value} onChange={()=>onChange({destinationMode:value as ActivationBuilderState["destinationMode"]})}/><span className="font-semibold">{label}</span></label>)}</div>{state.destinationMode==="fixed_destination"?<label className="mt-4 block"><span className="text-sm font-bold">Destino</span><select value={state.defaultDestinationId} onChange={e=>onChange({defaultDestinationId:e.target.value})} className="mt-2 min-h-12 w-full rounded-xl border border-[#dcdbe3] bg-white px-4"><option value="">Selecione</option>{destinations.map(item=><option key={item.id} value={item.id}>{item.name}</option>)}</select></label>:null}<p className="mt-3 text-sm text-[#73737c]">O WhatsApp continua iniciado pelo visitante e recebe o contexto da ativação.</p></fieldset>:null}</div>}
