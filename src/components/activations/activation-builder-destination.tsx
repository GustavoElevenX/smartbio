import type{ActivationBuilderState}from"./activation-builder-types";export function ActivationBuilderDestination({state,onChange}:{state:ActivationBuilderState;onChange:(patch:Partial<ActivationBuilderState>)=>void}){return<label className="block"><span className="text-sm font-bold">Canal de conclusão</span><select value={state.completionChannel} onChange={(e)=>onChange({completionChannel:e.target.value as ActivationBuilderState["completionChannel"]})} className="mt-2 min-h-12 w-full rounded-xl border border-[#dcdbe3] bg-white px-4"><option value="whatsapp">WhatsApp humano</option><option value="native">Ação nativa</option><option value="external_url">Link externo</option><option value="email">E-mail</option><option value="phone">Telefone</option></select><p className="mt-3 text-sm text-[#73737c]">O WhatsApp continua iniciado pelo visitante e recebe o contexto da ativação.</p></label>}
