import type{ActivationBuilderState}from"./activation-builder-types";export function ActivationBuilderPreview({state}:{state:ActivationBuilderState}){const benefit=state.offerType==='percentage_discount'&&state.percentage?`${state.percentage}% OFF`:state.offerType==='fixed_discount'&&state.amount?`R$ ${state.amount} OFF`:state.offerType==='free_shipping'?'Frete grátis':state.offerType==='no_discount'?'Sem desconto':'Benefício';return<div className="overflow-hidden rounded-2xl border border-[#dedce8] bg-white"><div className="bg-[#5b42df] px-4 py-3 text-center text-sm font-bold text-white">{benefit} · {state.endsAt?'por tempo limitado':'saiba mais'}</div><div className="p-6"><p className="text-sm font-bold text-[#6553d9]">Prévia da ativação</p><h3 className="mt-3 text-3xl font-extrabold tracking-[-.04em]">{state.title||state.name||'Sua ativação'}</h3><p className="mt-3 text-[#6e6e78]">{state.message||'Adicione uma mensagem clara para o visitante.'}</p><button type="button" className="mt-5 min-h-12 rounded-xl bg-[#17171c] px-5 font-bold text-white">{state.eligibility==='any'?'Continuar':'Verificar meu benefício'}</button></div></div>}
