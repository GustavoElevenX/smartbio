import type{ActivationBuilderState}from"./activation-builder-types";const offers=[['percentage_discount','Percentual'],['fixed_discount','Valor fixo'],['free_shipping','Frete grátis'],['special_price','Preço especial'],['free_item','Item grátis'],['bonus','Bônus'],['no_discount','Sem desconto']]as const;export function ActivationBuilderOffer({state,onChange}:{state:ActivationBuilderState;onChange:(patch:Partial<ActivationBuilderState>)=>void}){return<div><div className="grid gap-3 sm:grid-cols-2">{offers.map(([value,label])=><button key={value} type="button" onClick={()=>onChange({offerType:value})} className={`min-h-12 rounded-xl border px-4 text-left font-bold ${state.offerType===value?'border-[#0054fc] bg-[#eaf3ff] text-[#0054fc]':'border-[#dedde5] bg-white'}`}>{label}</button>)}</div>{state.offerType==='percentage_discount'?<label className="mt-6 block"><span className="text-sm font-bold">Percentual</span><div className="mt-2 flex max-w-xs items-center rounded-xl border border-[#dcdbe3] bg-white px-4"><input type="number" min="0.01" max="100" value={state.percentage} onChange={(e)=>onChange({percentage:e.target.value})} className="min-h-12 w-full outline-none"/><span>%</span></div></label>:null}{state.offerType==='fixed_discount'?<label className="mt-6 block"><span className="text-sm font-bold">Valor do desconto</span><input type="number" min="0" value={state.amount} onChange={(e)=>onChange({amount:e.target.value})} className="mt-2 min-h-12 w-full max-w-xs rounded-xl border border-[#dcdbe3] px-4"/></label>:null}{['percentage_discount','fixed_discount','free_shipping'].includes(state.offerType)?<label className="mt-5 block"><span className="text-sm font-bold">Pedido mínimo (opcional)</span><input type="number" min="0" value={state.minSubtotal} onChange={(e)=>onChange({minSubtotal:e.target.value})} className="mt-2 min-h-12 w-full max-w-xs rounded-xl border border-[#dcdbe3] px-4"/></label>:null}</div>}
