"use client";
import { Loader2, UploadCloud } from "lucide-react";
import { useState } from "react";
export function MediaUpload({ projectId, onUploaded }: { projectId: string; onUploaded: () => void }) { const [busy, setBusy] = useState(false); const [error, setError] = useState(""); async function upload(files: FileList | null) { if (!files?.length) return; setBusy(true); setError(""); try { for (const file of Array.from(files)) { const form = new FormData(); form.set("file", file); const response = await fetch(`/api/projects/${projectId}/media`, { method: "POST", body: form }); const payload = await response.json(); if (!response.ok) throw new Error(payload.error?.message || payload.error || `Falha ao enviar ${file.name}.`); } onUploaded(); } catch (caught) { setError(caught instanceof Error ? caught.message : "Falha no upload."); } finally { setBusy(false); } } return <label onDragOver={(event) => event.preventDefault()} onDrop={(event) => { event.preventDefault(); void upload(event.dataTransfer.files); }} className="focus-within:ring-4 flex min-h-40 cursor-pointer flex-col items-center justify-center rounded-[22px] border border-dashed border-[#cbc7df] bg-[#f7fbff] p-6 text-center"><span className="grid size-11 place-items-center rounded-[14px] bg-white text-[#0054fc] shadow-sm">{busy ? <Loader2 className="animate-spin" /> : <UploadCloud />}</span><strong className="mt-3 text-sm">{busy ? "Processando mídia" : "Arraste ou selecione arquivos"}</strong><p className="mt-1 text-xs text-[#777781]">PNG, JPEG, WebP, MP4 ou WebM · até 20 MB</p>{error ? <p className="mt-2 text-xs font-semibold text-red-600">{error}</p> : null}<input type="file" multiple className="sr-only" disabled={busy} accept="image/png,image/jpeg,image/webp,video/mp4,video/webm" onChange={(event) => { void upload(event.target.files); event.currentTarget.value = ""; }} /></label>; }
