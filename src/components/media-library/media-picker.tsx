"use client";
import { ImageIcon } from "lucide-react";
import { useEffect, useState } from "react";
import { MediaGrid, type MediaView } from "@/components/media-library/media-grid";
import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogFooter, DialogHeader, DialogTitle, DialogTrigger } from "@/components/ui/dialog";
export function MediaPicker({ projectId, value, onChange, label = "Selecionar mídia" }: { projectId: string; value?: string; onChange: (assetId?: string) => void; label?: string }) { const [open, setOpen] = useState(false); const [assets, setAssets] = useState<MediaView[]>([]); const [selected, setSelected] = useState<string[]>(value ? [value] : []); useEffect(() => { if (!open) return; fetch(`/api/projects/${projectId}/media`).then((response) => response.json()).then((payload) => setAssets((payload.data || []).filter((asset: MediaView) => asset.mimeType.startsWith("image/")))).catch(() => undefined); }, [open, projectId]); return <Dialog open={open} onOpenChange={setOpen}><DialogTrigger asChild><Button type="button" size="sm" variant="secondary"><ImageIcon size={14} />{value ? "Trocar mídia" : label}</Button></DialogTrigger><DialogContent className="max-h-[85vh] overflow-y-auto sm:max-w-4xl"><DialogHeader><DialogTitle>Selecionar da biblioteca</DialogTitle></DialogHeader><MediaGrid assets={assets} selected={selected} onSelect={(asset) => setSelected([asset.id])} /><DialogFooter><Button variant="ghost" onClick={() => { onChange(undefined); setSelected([]); setOpen(false); }}>Remover seleção</Button><Button disabled={!selected[0]} onClick={() => { onChange(selected[0]); setOpen(false); }}>Usar asset</Button></DialogFooter></DialogContent></Dialog>; }
