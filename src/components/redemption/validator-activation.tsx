"use client";import{useEffect,useState}from"react";import{useRouter}from"next/navigation";import{Loader2}from"lucide-react";export function ValidatorActivation({token}:{token:string}){const router=useRouter();const[error,setError]=useState("");useEffect(()=>{void fetch('/api/redeem/activate',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({token})}).then(async(response)=>{if(!response.ok){const payload=await response.json()as{error?:{message?:string}};throw new Error(payload.error?.message||'Link inválido.')}router.replace('/redeem');router.refresh();}).catch((cause)=>setError(cause instanceof Error?cause.message:'Link inválido.'))},[router,token]);return<div className="grid min-h-screen place-items-center bg-[#f7f7fa] p-6 text-center"><div>{error?<><h1 className="text-2xl font-black">Não foi possível ativar este validador.</h1><p className="mt-3 text-red-700">{error}</p></>:<><Loader2 className="mx-auto animate-spin text-[#6048df]"/><h1 className="mt-4 text-xl font-black">Ativando dispositivo seguro…</h1></>}</div></div>}
