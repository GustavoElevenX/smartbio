import{CheckCircle2,MapPin,XCircle}from"lucide-react";export interface ValidatedBenefit{valid:boolean;claim:{code:string;benefitLabel:string;expiresAt?:string;phoneMasked?:string;activationName?:string;allowedLocationName?:string;status:string};validator?:{name:string;locationId?:string}}export function BenefitValidationCard({benefit}:{benefit:ValidatedBenefit}){const valid=benefit.valid;return<section className={`rounded-2xl border p-5 ${valid?'border-emerald-200 bg-emerald-50/50':'border-[#ffc6bd] bg-[#fff6f4]'}`}><div className="flex items-center gap-2">{valid?<CheckCircle2 className="text-emerald-700"/>:<XCircle className="text-[#ef604c]"/>}<strong className={valid?'text-emerald-800':'text-[#c64735]'}>{valid?'Benefício válido':benefit.claim.status==='redeemed'?'Código já utilizado':'Benefício indisponível'}</strong></div><h2 className="mt-5 text-3xl font-black tracking-[-.04em]">{benefit.claim.benefitLabel}</h2><p className="mt-1 text-[#666670]">{benefit.claim.activationName}</p><div className="mt-5 flex flex-wrap items-center justify-between gap-3 border-t border-current/10 pt-4 text-sm"><span className="inline-flex items-center gap-2"><MapPin size={16}/>{benefit.claim.allowedLocationName||'Unidade autorizada'}</span>{benefit.claim.phoneMasked?<span>{benefit.claim.phoneMasked}</span>:null}</div></section>}
