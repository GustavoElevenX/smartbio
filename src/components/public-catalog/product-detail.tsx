import Image from "next/image";
import { X } from "lucide-react";
import type { PublicCatalogItem } from "./catalog.types";

export function ProductDetail({ item, onClose, onChoose }: { item: PublicCatalogItem; onClose(): void; onChoose(): void }) {
  return <div role="dialog" aria-modal="true" aria-labelledby="catalog-product-title" className="fixed inset-0 z-50 grid items-end bg-black/45 md:items-center md:p-6"><button type="button" aria-label="Fechar detalhes" className="absolute inset-0" onClick={onClose} /><section className="relative z-10 mx-auto max-h-[92dvh] w-full max-w-3xl overflow-auto rounded-t-[32px] bg-white p-6 shadow-2xl md:rounded-[32px]"><button type="button" onClick={onClose} aria-label="Fechar" className="absolute right-4 top-4 z-10 grid size-11 place-items-center rounded-full bg-white shadow"><X aria-hidden /></button><div className="grid gap-7 md:grid-cols-2">{item.imageUrl ? <div className="relative aspect-square overflow-hidden rounded-3xl bg-black/5"><Image src={item.imageUrl} alt={item.name} fill unoptimized sizes="50vw" className="object-cover" /></div> : <div className="aspect-square rounded-3xl bg-black/5" />}<div className="self-center"><p className="text-sm font-bold text-[var(--presence-primary)]">{item.categoryName}</p><h2 id="catalog-product-title" className="mt-2 text-3xl font-black tracking-[-.04em]">{item.name}</h2>{item.description ? <p className="mt-4 leading-7 text-black/60">{item.description}</p> : null}{item.variants.length ? <p className="mt-4 text-sm text-black/55">{item.variants.length} opções disponíveis</p> : null}<button type="button" onClick={onChoose} className="mt-7 min-h-12 w-full rounded-2xl bg-[var(--presence-primary)] px-5 font-extrabold text-white">Quero este produto</button></div></div></section></div>;
}
