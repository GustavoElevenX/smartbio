"use client";import type{PublicActivation}from"@/features/activations/activation.types";export function ActivationAnnouncementBar({activation,content,onClick}:{activation:PublicActivation;content:Record<string,unknown>;onClick:()=>void}){return<div className="relative z-50 bg-[var(--presence-primary)] px-4 py-3 text-white"><div className="mx-auto flex max-w-7xl items-center justify-center gap-4 text-center text-sm"><strong>{String(content.title||activation.title||activation.offer?.label||activation.name)}</strong>{activation.endsAt?<span className="hidden text-white/75 sm:inline">até {new Intl.DateTimeFormat('pt-BR',{dateStyle:'medium'}).format(new Date(activation.endsAt))}</span>:null}<button type="button" onClick={onClick} className="min-h-10 rounded-full border border-white/40 px-4 font-extrabold hover:bg-white/10">{String(content.ctaLabel||'Quero aproveitar')}</button></div></div>}
