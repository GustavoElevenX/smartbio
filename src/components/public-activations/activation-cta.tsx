"use client";import{ArrowRight}from"lucide-react";import{useActivationRuntime}from"./activation-runtime-provider";export function ActivationCta({activationId,label,className=""}:{activationId:string;label:string;className?:string}){const runtime=useActivationRuntime();return<button type="button" onClick={()=>runtime?.startActivation(activationId)} className={`inline-flex min-h-12 items-center gap-2 rounded-[var(--presence-button-radius)] bg-[var(--presence-primary)] px-5 font-extrabold text-white ${className}`}>{label}<ArrowRight size={17}/></button>}
