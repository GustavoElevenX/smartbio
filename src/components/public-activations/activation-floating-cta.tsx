"use client";import type{PublicActivation}from"@/features/activations/activation.types";export function ActivationFloatingCta({activation,content,onClick}:{activation:PublicActivation;content:Record<string,unknown>;onClick:()=>void}){return<button type="button" onClick={onClick} className="fixed bottom-4 left-4 right-4 z-40 min-h-14 rounded-[var(--presence-button-radius)] bg-[var(--presence-primary)] px-5 font-extrabold text-white shadow-[var(--presence-floating-shadow)] md:left-auto md:right-6 md:w-auto">{String(content.ctaLabel||activation.offer?.label||activation.title||activation.name)}</button>}
