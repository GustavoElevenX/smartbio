import { EntryPointLink } from "./entry-point-link";
import type { ConversionGoal, EntryPoint } from "@/types";
import { entryChannelPreset } from "@/features/entry-points/presets";
export function EntryPointCard({ entry, goal, url }: { entry: EntryPoint; goal?: ConversionGoal; url: string }) { return <article className="rounded-[20px] border border-[#e2e0e8] bg-white p-5"><div className="flex items-start justify-between gap-3"><div><span className="text-[10px] font-bold uppercase tracking-[.12em] text-[#77747f]">{entryChannelPreset(entry.channel).label}</span><h3 className="mt-1 font-extrabold">{entry.name}</h3><p className="mt-2 text-xs text-[#77747f]">{goal ? `Meta: ${goal.name}` : "Usar CTAs da página"}</p></div><span className={`rounded-full px-2 py-1 text-[10px] font-bold ${entry.isActive ? "bg-emerald-50 text-emerald-700" : "bg-zinc-100 text-zinc-500"}`}>{entry.isActive ? "Ativa" : "Pausada"}</span></div><div className="mt-5"><EntryPointLink url={url} /></div></article>; }
