export function EntryPointQr({ url, name }: { url: string; name: string }) { const source = `https://api.qrserver.com/v1/create-qr-code/?size=220x220&format=svg&data=${encodeURIComponent(url)}`; return <div className="rounded-[18px] border border-[#e4e2e9] bg-white p-4 text-center"><img src={source} alt={`QR code para ${name}`} width={160} height={160} className="mx-auto size-40" /><a href={source} download={`qr-${name.toLowerCase().replace(/\s+/g, "-")}.svg`} className="mt-3 inline-flex text-xs font-bold text-[#0054fc]">Baixar QR em SVG</a></div>; }
