import type { ConversionGoal, EntryPoint } from "@/types";
export function EntryPointPreview({ entry, goal }: { entry: EntryPoint; goal?: ConversionGoal }) { return <div className="rounded-[18px] bg-[#19191e] p-5 text-white"><span className="text-[10px] font-bold uppercase tracking-[.14em] text-white/50">Ao abrir · {entry.channel}</span><h3 className="mt-6 text-xl font-extrabold tracking-[-.03em]">{goal?.name || "Etapa direta"}</h3><p className="mt-2 text-xs leading-5 text-white/60">{goal?.description || "O visitante inicia no destino configurado."}</p><div className="mt-6 h-1.5 overflow-hidden rounded-full bg-white/10"><div className="h-full w-1/3 rounded-full bg-[#8e7dff]" /></div></div>; }
