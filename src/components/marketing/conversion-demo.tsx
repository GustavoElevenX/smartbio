"use client";
import { ArrowRight, Check, MessageCircle, ShoppingBag, Target } from "lucide-react";
import { useState } from "react";
const goals = [{ name: "Pedir agora", icon: ShoppingBag, path: "Cardápio → itens → pedido" }, { name: "Comprar para revenda", icon: Target, path: "Volume → perfil → atendimento" }, { name: "Falar com a equipe", icon: MessageCircle, path: "Contexto → destino → conversa" }];
export function ConversionDemo() { const [selected, setSelected] = useState(0); return <div className="relative mx-auto w-full max-w-[520px]"><div className="absolute -inset-7 rotate-3 rounded-[42px] bg-[#ded8ff]" /><div className="relative rounded-[30px] border border-[#dedbe4] bg-white p-5 shadow-[0_32px_90px_rgba(41,34,76,.16)] sm:p-7"><div className="flex items-center justify-between"><span className="text-xs font-extrabold">CASA DE SUCOS</span><span className="rounded-full bg-emerald-50 px-2.5 py-1 text-[10px] font-bold text-emerald-700">Entrada: Bio</span></div><div className="mt-12"><span className="text-[10px] font-bold uppercase tracking-[.16em] text-[#6556d7]">Intenção</span><h2 className="mt-3 text-3xl font-extrabold tracking-[-.045em]">O que você deseja fazer hoje?</h2></div><div className="mt-7 grid gap-2">{goals.map((goal, index) => { const Icon = goal.icon; return <button key={goal.name} type="button" onClick={() => setSelected(index)} className={`flex min-h-16 items-center gap-3 rounded-[17px] border p-3 text-left transition ${selected === index ? "border-[#7566df] bg-[#f1efff]" : "border-[#e4e1e8]"}`}><span className="grid size-9 place-items-center rounded-xl bg-white"><Icon size={17} /></span><span className="flex-1"><strong className="block text-sm">{goal.name}</strong><small className="mt-1 block text-[#77737e]">{goal.path}</small></span>{selected === index ? <Check size={17} className="text-[#6252d3]" /> : <ArrowRight size={16} />}</button>; })}</div><div className="mt-7 flex items-center justify-between border-t border-[#ebe8ef] pt-4 text-[10px] font-bold text-[#77737e]"><span>Meta resolvida: {goals[selected].name}</span><span>Feito com Virou</span></div></div></div>; }
