"use client";import{useEffect,useState}from"react";import Link from"next/link";import type{WorkspaceEntitlements}from"@/server/entitlements/entitlement-types";export function BillingSettingsReal(){const[data,setData]=useState<WorkspaceEntitlements>();useEffect(()=>{void fetch('/api/workspace/entitlements').then(r=>r.json()).then(x=>setData(x.data));},[]);return<div className="mx-auto max-w-5xl"><h1 className="text-3xl font-extrabold">Plano e acesso</h1><section className="mt-7 overflow-hidden rounded-3xl border bg-white"><div className="bg-[#1c1a24] p-7 text-white"><span className="rounded-full bg-[#6d5ef5] px-3 py-1.5 text-xs font-bold">{data?.plan.name||'Carregando…'}</span><h2 className="mt-5 text-3xl font-extrabold">Seu acesso na Virou</h2><p className="mt-2 text-white/60">{data?`Origem: ${data.plan.source==='manual'?'Concedido pela Virou':data.plan.source}. Status: ${data.plan.status}.`:'Consultando entitlements…'}</p></div><div className="p-7"><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{Object.entries(data?.features||{}).filter(([,v])=>v.enabled).map(([key,value])=><div key={key} className="rounded-2xl bg-[#f5f4f8] p-4"><strong className="text-sm">{key}</strong><p className="mt-1 text-xs text-[#76747e]">{value.limit==null?'Incluído':`${value.used||0} / ${value.limit}`}</p></div>)}</div><div className="mt-6 rounded-2xl border border-amber-200 bg-amber-50 p-5"><strong>Pagamento online ainda não está configurado.</strong><p className="mt-1 text-sm text-amber-900">Seu plano é gerenciado pela equipe da Virou durante o beta.</p></div><Link href="/pricing" className="mt-5 inline-flex min-h-11 items-center rounded-xl bg-[#17171c] px-4 text-sm font-bold text-white">Comparar planos</Link></div></section></div>}
