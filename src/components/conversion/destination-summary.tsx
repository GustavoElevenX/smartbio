import type { Project } from "@/types";
export function DestinationSummary({ project }: { project: Project }) { const destinations = project.commercialConfig?.routingDestinations || []; return <div className="rounded-[20px] border border-[#e2e0e8] bg-[#0054fc] p-5 text-white"><span className="text-xs font-bold text-white/55">Destinos configurados</span><strong className="mt-2 block text-3xl">{destinations.length}</strong><p className="mt-4 text-xs leading-5 text-white/60">{destinations.length ? destinations.map((item) => item.label).slice(0, 3).join(" · ") : `Destino principal: ${project.primaryDestination}`}</p></div>; }
