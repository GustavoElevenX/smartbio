"use client";
import Link from "next/link";
import { NotificationItem } from "@/components/notifications/notification-item";
import type { Notification } from "@/types";
export function NotificationPopover({ items, onRead, onReadAll }: { items: Notification[]; onRead: (id: string) => void; onReadAll: () => void }) { return <div className="w-[min(390px,calc(100vw-24px))]"><div className="flex items-center justify-between border-b border-[#e8e7ee] p-4"><strong>Notificações</strong><button type="button" className="text-xs font-bold text-[#5d50c7]" onClick={onReadAll}>Marcar todas</button></div><div className="flex max-h-[430px] flex-col gap-2 overflow-y-auto p-3">{items.length ? items.slice(0, 8).map((item) => <NotificationItem key={item.id} item={item} compact onRead={onRead} />) : <p className="p-6 text-center text-sm text-[#777781]">Nenhuma notificação.</p>}</div><Link href="/app/notifications" className="block border-t border-[#e8e7ee] p-3 text-center text-xs font-bold text-[#5d50c7]">Ver todas</Link></div>; }
