"use client";
import { Input, Label, Textarea } from "@/components/ui/field";
import type { ProjectPolicy } from "@/types";
export function ReservationPolicyForm({ projectId, policy, onChange }: { projectId: string; policy?: ProjectPolicy; onChange: (value: ProjectPolicy) => void }) { const value = policy || { id: crypto.randomUUID(), projectId, type: "reservation" as const, title: "Política de reserva", content: "", isActive: true, settings: {} }; return <div className="rounded-[18px] border border-amber-200 bg-amber-50 p-4"><h3 className="font-extrabold">Política de reserva</h3><p className="mb-4 mt-1 text-xs leading-5 text-amber-900/70">Revise com orientação jurídica adequada. A SmartBio não oferece garantia jurídica.</p><div className="grid gap-3"><div><Label htmlFor="reservation-policy-title">Título</Label><Input id="reservation-policy-title" value={value.title} onChange={(event) => onChange({ ...value, title: event.target.value })} /></div><div><Label htmlFor="reservation-policy-content">Conteúdo confirmado</Label><Textarea id="reservation-policy-content" value={value.content} onChange={(event) => onChange({ ...value, content: event.target.value })} /></div></div></div>; }
