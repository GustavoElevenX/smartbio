import type { ReservableUnit } from "@/types";
export function ReservationPreview({ units }: { units: ReservableUnit[] }) { return <div className="rounded-[18px] bg-[#f5f4f8] p-4"><h3 className="text-sm font-extrabold">Preview da disponibilidade</h3><div className="mt-3 grid gap-2 sm:grid-cols-2">{units.filter((unit) => unit.isActive).map((unit) => <div key={unit.id} className="rounded-xl bg-white p-3"><strong className="text-sm">{unit.name || "Unidade sem nome"}</strong><p className="mt-1 text-xs text-[#777781]">Até {unit.capacityAdults} adulto(s) e {unit.capacityChildren} criança(s) · {unit.quantity} unidade(s)</p></div>)}</div></div>; }
