"use client";
import { Input, Label, Select } from "@/components/ui/field";
import { WhatsAppMessageBuilder } from "@/components/commercial-data/destinations/whatsapp-message-builder";
import type { RoutingDestination } from "@/types";
export function DestinationForm({ value, onChange }: { value: RoutingDestination; onChange: (value: RoutingDestination) => void }) { const patch = (next: Partial<RoutingDestination>) => onChange({ ...value, ...next }); return <div className="grid gap-4"><div className="grid gap-4 sm:grid-cols-2"><div><Label htmlFor={`destination-label-${value.id}`}>Nome</Label><Input id={`destination-label-${value.id}`} value={value.label} onChange={(event) => patch({ label: event.target.value, key: value.key || event.target.value.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "") })} /></div><div><Label htmlFor={`destination-type-${value.id}`}>Canal</Label><Select id={`destination-type-${value.id}`} value={value.type} onChange={(event) => patch({ type: event.target.value as RoutingDestination["type"] })}><option value="whatsapp">WhatsApp</option><option value="url">URL</option><option value="email">E-mail</option><option value="phone">Telefone</option><option value="checkout">Checkout</option><option value="schedule">Agenda externa</option><option value="form">Formulário</option><option value="seller">Vendedor</option><option value="location">Unidade</option><option value="unavailable">Fallback indisponível</option></Select></div></div><div><Label htmlFor={`destination-value-${value.id}`}>{value.type === "whatsapp" || value.type === "phone" ? "Telefone" : value.type === "email" ? "E-mail" : "Valor ou URL"}</Label><Input id={`destination-value-${value.id}`} type={value.type === "email" ? "email" : value.type === "url" || value.type === "checkout" || value.type === "schedule" ? "url" : "text"} value={value.value || ""} onChange={(event) => patch({ value: event.target.value })} /></div>{value.type === "whatsapp" ? <WhatsAppMessageBuilder value={value.message || ""} onChange={(message) => patch({ message })} /> : null}</div>; }
