"use client";
import { Input, Label } from "@/components/ui/field";
export function PaymentDestinationForm({ value, onChange }: { value?: string; onChange: (value?: string) => void }) { return <div className="rounded-[18px] border border-[#eaf3ff] bg-[#f7fbff] p-4"><h3 className="font-extrabold">Pagamento externo</h3><p className="mb-4 mt-1 text-xs text-[#74747e]">Use apenas checkout HTTPS real. A Sobe não simula pagamentos.</p><Label htmlFor="payment-url">URL do checkout</Label><Input id="payment-url" type="url" value={value || ""} onChange={(event) => onChange(event.target.value || undefined)} placeholder="https://" /></div>; }
