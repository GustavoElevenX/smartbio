"use client";
import { Plus, Trash2 } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Input, Label, Select } from "@/components/ui/field";
import { Switch } from "@/components/ui/switch";
import type { OpeningHoursRule } from "@/types";
const days = ["Domingo", "Segunda", "Terça", "Quarta", "Quinta", "Sexta", "Sábado"];
export function OpeningHoursEditor({ value, onChange }: { value: OpeningHoursRule[]; onChange: (value: OpeningHoursRule[]) => void }) { return <div><div className="mb-3 flex items-center justify-between"><div><Label>Horários</Label><p className="text-xs text-[#777781]">Início e fim iguais representam 24 horas.</p></div><Button type="button" size="sm" variant="secondary" onClick={() => onChange([...value, { weekday: 1, opensAt: "09:00", closesAt: "18:00" }])}><Plus size={15} />Horário</Button></div><div className="flex flex-col gap-2">{value.map((rule, index) => <div key={`${rule.weekday}-${index}`} className="grid gap-2 rounded-[14px] bg-[#f6f5f9] p-3 sm:grid-cols-[1fr_120px_120px_auto_auto]"><Select aria-label="Dia" value={rule.weekday} onChange={(event) => onChange(value.map((item, itemIndex) => itemIndex === index ? { ...item, weekday: Number(event.target.value) } : item))}>{days.map((day, dayIndex) => <option key={day} value={dayIndex}>{day}</option>)}</Select><Input aria-label="Abre às" type="time" value={rule.opensAt} disabled={rule.isClosed} onChange={(event) => onChange(value.map((item, itemIndex) => itemIndex === index ? { ...item, opensAt: event.target.value } : item))} /><Input aria-label="Fecha às" type="time" value={rule.closesAt} disabled={rule.isClosed} onChange={(event) => onChange(value.map((item, itemIndex) => itemIndex === index ? { ...item, closesAt: event.target.value } : item))} /><label className="flex items-center gap-2 text-xs font-semibold"><Switch checked={Boolean(rule.isClosed)} onCheckedChange={(isClosed) => onChange(value.map((item, itemIndex) => itemIndex === index ? { ...item, isClosed } : item))} />Fechado</label><Button type="button" size="icon" variant="ghost" aria-label="Excluir horário" onClick={() => onChange(value.filter((_, itemIndex) => itemIndex !== index))}><Trash2 size={15} /></Button></div>)}</div></div>; }
