import { MapPin } from "lucide-react";
import type { BusinessLocation } from "@/types";
export function LocationMapPreview({ location }: { location: BusinessLocation }) { const resolved = location.latitude != null && location.longitude != null; return <div className="rounded-[18px] border border-[#e4e3ea] bg-[#f6f5f9] p-4"><div className="flex items-start gap-3"><span className="grid size-10 shrink-0 place-items-center rounded-xl bg-white text-[#0054fc]"><MapPin size={19} /></span><div><strong className="text-sm">{resolved ? "Coordenadas registradas" : "Endereço ainda não geocodificado"}</strong><p className="mt-1 text-xs text-[#777781]">{resolved ? `${location.latitude?.toFixed(6)}, ${location.longitude?.toFixed(6)} · ${location.geocodingStatus}` : "Geocodifique ou informe latitude e longitude manualmente."}</p>{resolved ? <a className="mt-2 inline-flex text-xs font-bold text-[#0054fc]" href={`https://www.google.com/maps?q=${location.latitude},${location.longitude}`} target="_blank" rel="noreferrer">Conferir no mapa ↗</a> : null}</div></div></div>; }
