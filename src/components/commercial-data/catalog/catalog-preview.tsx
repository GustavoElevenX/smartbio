import type { CatalogItem } from "@/types";
export function CatalogPreview({ items }: { items: CatalogItem[] }) { const available = items.filter((item) => item.isAvailable); return <div className="rounded-[18px] bg-[#f5f4f8] p-4"><h3 className="text-sm font-extrabold">Preview do catálogo</h3>{available.length ? <div className="mt-3 grid gap-2 sm:grid-cols-2">{available.map((item) => <div key={item.id} className="rounded-xl bg-white p-3"><strong className="text-sm">{item.name || "Produto sem nome"}</strong><p className="mt-1 text-xs text-[#74747e]">{item.price == null ? "Preço não confirmado" : new Intl.NumberFormat("pt-BR", { style: "currency", currency: item.currency }).format(item.price)}</p></div>)}</div> : <p className="mt-2 text-xs text-[#74747e]">Nenhum item disponível.</p>}</div>; }
