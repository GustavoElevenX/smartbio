import { AlertCircle, CheckCircle2 } from "lucide-react";
import type { AvailabilityRule, SchedulableService } from "@/types";
export function SchedulingPreview({ services, rules }: { services: SchedulableService[]; rules: AvailabilityRule[] }) { const valid = services.some((item) => item.isActive && item.durationMinutes > 0) && rules.some((item) => item.timezone && item.startTime && item.endTime); return <div className={`flex items-start gap-3 rounded-[18px] p-4 ${valid ? "bg-emerald-50 text-emerald-900" : "bg-amber-50 text-amber-950"}`}>{valid ? <CheckCircle2 size={19} /> : <AlertCircle size={19} />}<div><strong className="text-sm">{valid ? "Agenda pronta para publicação" : "Agenda ainda incompleta"}</strong><p className="mt-1 text-xs leading-5 opacity-75">É necessário serviço ativo, duração, confirmação, disponibilidade e timezone.</p></div></div>; }
