"use client";
import { Plus, Trash2 } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Input, Label, Select } from "@/components/ui/field";
import type { AvailabilityRule } from "@/types";
const weekdays = ["Domingo", "Segunda", "Terça", "Quarta", "Quinta", "Sexta", "Sábado"];
export function AvailabilityWeek({ projectId, rules, onChange }: { projectId: string; rules: AvailabilityRule[]; onChange: (value: AvailabilityRule[]) => void }) { function add() { onChange([...rules, { id: crypto.randomUUID(), projectId, weekday: 1, startTime: "09:00", endTime: "18:00", timezone: "America/Sao_Paulo" }]); } return <section><div className="mb-3 flex items-center justify-between"><div><h3 className="font-extrabold">Disponibilidade semanal</h3><p className="text-xs text-[#797983]">Horários confirmados e fuso por regra.</p></div><Button size="sm" variant="secondary" onClick={add}><Plus data-icon="inline-start" />Adicionar</Button></div><div className="flex flex-col gap-3">{rules.map((rule) => <div key={rule.id} className="grid gap-3 rounded-[16px] border border-[#e5e4eb] p-3 sm:grid-cols-[1fr_130px_130px_1fr_auto]"><div><Label htmlFor={`day-${rule.id}`}>Dia</Label><Select id={`day-${rule.id}`} value={rule.weekday} onChange={(event) => onChange(rules.map((item) => item.id === rule.id ? { ...item, weekday: Number(event.target.value) } : item))}>{weekdays.map((day, index) => <option key={day} value={index}>{day}</option>)}</Select></div><div><Label htmlFor={`start-${rule.id}`}>Início</Label><Input id={`start-${rule.id}`} type="time" value={rule.startTime} onChange={(event) => onChange(rules.map((item) => item.id === rule.id ? { ...item, startTime: event.target.value } : item))} /></div><div><Label htmlFor={`end-${rule.id}`}>Fim</Label><Input id={`end-${rule.id}`} type="time" value={rule.endTime} onChange={(event) => onChange(rules.map((item) => item.id === rule.id ? { ...item, endTime: event.target.value } : item))} /></div><div><Label htmlFor={`tz-${rule.id}`}>Timezone</Label><Input id={`tz-${rule.id}`} value={rule.timezone} onChange={(event) => onChange(rules.map((item) => item.id === rule.id ? { ...item, timezone: event.target.value } : item))} /></div><Button aria-label="Excluir regra" title="Excluir regra" size="icon" variant="ghost" className="self-end" onClick={() => onChange(rules.filter((item) => item.id !== rule.id))}><Trash2 size={16} /></Button></div>)}</div></section>; }
