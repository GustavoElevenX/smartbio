import type { CommercialOpportunity, ConversionGoal, EntryPoint } from "@/types";
import { OpportunityStatusBadge } from "./opportunity-status";
export function OpportunityDetails({ opportunity, goal, entry }: { opportunity: CommercialOpportunity; goal?: ConversionGoal; entry?: EntryPoint }) { return <div><div className="flex items-start justify-between gap-4"><div><span className="text-xs font-bold text-[#0054fc]">{opportunity.projectName}</span><h2 className="mt-1 text-2xl font-extrabold tracking-[-.035em]">{opportunity.title}</h2></div><OpportunityStatusBadge status={opportunity.status} /></div>{opportunity.activationId||opportunity.benefitClaimId?<div className="mt-4 flex flex-wrap gap-2">{opportunity.activationId?<span className="rounded-full bg-[#eaf3ff] px-3 py-1 text-xs font-bold text-[#0054fc]">Ativação atribuída</span>:null}{opportunity.benefitClaimId?<span className="rounded-full bg-emerald-50 px-3 py-1 font-mono text-xs font-bold text-emerald-700">Benefício · {String(opportunity.metadata.benefitClaimCode||opportunity.benefitClaimId).slice(0,8)}</span>:null}</div>:null}<dl className="mt-6 grid gap-3 rounded-[18px] bg-[#f6f5f8] p-4 text-sm sm:grid-cols-2"><div><dt className="text-xs text-[#7b7883]">Contato</dt><dd className="mt-1 font-semibold">{opportunity.contactName || "Não informado"}</dd></div><div><dt className="text-xs text-[#7b7883]">Origem</dt><dd className="mt-1 font-semibold">{opportunity.attribution?.source || "Direto"}</dd></div><div><dt className="text-xs text-[#7b7883]">Meta</dt><dd className="mt-1 font-semibold">{goal?.name || "Não atribuída"}</dd></div><div><dt className="text-xs text-[#7b7883]">Entrada</dt><dd className="mt-1 font-semibold">{entry?.name || "Acesso geral"}</dd></div></dl>{opportunity.summary ? <div className="mt-5"><h3 className="text-xs font-bold uppercase tracking-[.12em] text-[#77747f]">Contexto</h3><p className="mt-2 text-sm leading-6">{opportunity.summary}</p></div> : null}</div>; }
