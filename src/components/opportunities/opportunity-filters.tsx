import type { OpportunityStatus } from "@/types";
export function OpportunityFilters({ value, onChange }: { value: "all" | OpportunityStatus; onChange: (value: "all" | OpportunityStatus) => void }) { return <select aria-label="Filtrar oportunidades por status" value={value} onChange={(event) => onChange(event.target.value as "all" | OpportunityStatus)} className="min-h-11 rounded-xl border border-[#dedce5] bg-white px-3 text-xs font-bold"><option value="all">Todos os status</option><option value="new">Novas</option><option value="in_progress">Em andamento</option><option value="converted">Convertidas</option><option value="lost">Perdidas</option><option value="archived">Arquivadas</option></select>; }
