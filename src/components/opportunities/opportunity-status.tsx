import type { OpportunityStatus } from "@/types";
const labels: Record<OpportunityStatus, string> = { new: "Nova", in_progress: "Em andamento", converted: "Convertida", lost: "Perdida", archived: "Arquivada" };
export function OpportunityStatusBadge({ status }: { status: OpportunityStatus }) { const color = status === "converted" ? "bg-emerald-50 text-emerald-700" : status === "lost" ? "bg-red-50 text-red-700" : status === "in_progress" ? "bg-amber-50 text-amber-800" : "bg-[#eeeaff] text-[#5b4dcc]"; return <span className={`rounded-full px-2.5 py-1 text-[10px] font-bold ${color}`}>{labels[status]}</span>; }
