import { CalendarDays, CircleDollarSign } from "lucide-react";
import { OpportunityStatusBadge } from "./opportunity-status";
import type { CommercialOpportunity } from "@/types";
export function OpportunityCard({ opportunity, onSelect }: { opportunity: CommercialOpportunity; onSelect: () => void }) { return <button type="button" onClick={onSelect} className="w-full rounded-[19px] border border-[#e3e1e8] bg-white p-4 text-left transition hover:border-[#bdb7da] hover:shadow-[0_14px_35px_rgba(31,27,54,.07)]"><div className="flex items-start justify-between gap-3"><div><span className="text-[10px] font-bold uppercase tracking-[.12em] text-[#85828e]">{opportunity.sourceType.replace("_", " ")}</span><h3 className="mt-1 font-extrabold">{opportunity.title}</h3><p className="mt-1 text-xs text-[#74717d]">{opportunity.contactName || "Contato ainda não informado"}</p></div><OpportunityStatusBadge status={opportunity.status} /></div><div className="mt-5 flex flex-wrap gap-4 text-[11px] text-[#77747f]"><span className="flex items-center gap-1.5"><CalendarDays size={13} /> {new Intl.DateTimeFormat("pt-BR").format(new Date(opportunity.createdAt))}</span>{opportunity.estimatedValue != null ? <span className="flex items-center gap-1.5"><CircleDollarSign size={13} /> {new Intl.NumberFormat("pt-BR", { style: "currency", currency: opportunity.currency }).format(opportunity.estimatedValue)}</span> : null}</div></button>; }
