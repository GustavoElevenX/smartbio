import { z } from "zod";

export const activationOfferScopeSchema = z.object({ catalogItemIds: z.array(z.string().uuid()).optional(), catalogCategoryIds: z.array(z.string().uuid()).optional(), serviceOfferingIds: z.array(z.string().uuid()).optional(), schedulableServiceIds: z.array(z.string().uuid()).optional(), reservableUnitIds: z.array(z.string().uuid()).optional(), locationIds: z.array(z.string().uuid()).optional(), fulfillment: z.array(z.enum(["delivery","pickup","digital","external"])).optional() });
const activationOfferBaseSchema = z.object({ id: z.string().uuid(), activationId: z.string().uuid(), offerType: z.enum(["percentage_discount","fixed_discount","free_shipping","special_price","free_item","bonus","coupon","no_discount"]), label: z.string().trim().min(1).max(120), description: z.string().trim().max(500).optional(), percentage: z.number().positive().max(100).optional(), amount: z.number().nonnegative().optional(), specialPrice: z.number().nonnegative().optional(), currency: z.string().length(3).toUpperCase().default("BRL"), minSubtotal: z.number().nonnegative().optional(), maxDiscount: z.number().nonnegative().optional(), scope: activationOfferScopeSchema.default({}), benefitConfig: z.record(z.string(), z.unknown()).default({}), isActive: z.boolean().default(true), createdAt: z.iso.datetime().optional(), updatedAt: z.iso.datetime().optional() });
function validateOffer(offer: Pick<z.infer<typeof activationOfferBaseSchema>, "offerType" | "percentage" | "amount" | "specialPrice">, ctx: z.RefinementCtx) { if (offer.offerType === "percentage_discount" && offer.percentage == null) ctx.addIssue({ code: "custom", path: ["percentage"], message: "Informe o percentual." }); if (offer.offerType === "fixed_discount" && offer.amount == null) ctx.addIssue({ code: "custom", path: ["amount"], message: "Informe o valor fixo." }); if (offer.offerType === "special_price" && offer.specialPrice == null) ctx.addIssue({ code: "custom", path: ["specialPrice"], message: "Informe o preço especial." }); }
export const activationOfferSchema = activationOfferBaseSchema.superRefine(validateOffer);
export const activationOfferDraftSchema = activationOfferBaseSchema.omit({ id: true, activationId: true, createdAt: true, updatedAt: true }).superRefine(validateOffer);
