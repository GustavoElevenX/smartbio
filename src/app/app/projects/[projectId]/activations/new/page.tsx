import type{Metadata}from"next";import{ActivationBuilder}from"@/components/activations/activation-builder";import{requireAuthenticatedActor}from"@/server/auth/setup-actor";import{loadProjectForActor}from"@/server/projects/load-project-for-actor";export const metadata:Metadata={title:"Nova ativação · Virou"};export default async function Page({params}:PageProps<"/app/projects/[projectId]/activations/new">){const{projectId}=await params;const actor=await requireAuthenticatedActor();const project=await loadProjectForActor(actor,projectId);return<ActivationBuilder projectId={projectId} project={project}/>}
