import type{Metadata}from"next";import{ActivationsPage}from"@/components/activations/activations-page";import{ActivationService}from"@/server/activations/activation-service";import{createServiceClient}from"@/lib/supabase/server";export const metadata:Metadata={title:"Ativações · Virou"};export default async function Page({params}:PageProps<"/app/projects/[projectId]/activations">){const{projectId}=await params;const activations=await new ActivationService(createServiceClient()).list(projectId);return<ActivationsPage projectId={projectId} activations={activations}/>}
