import{createServiceClient}from"@/lib/supabase/server";import{PlatformAdminRepository}from"@/server/platform-admin/platform-admin-repository";export default async function Page(){const{metrics,funnel}=await new PlatformAdminRepository(createServiceClient()!).overview();const cards=[['Usuários',metrics.usersTotal],['Workspaces',metrics.workspacesTotal],['Negócios',metrics.projectsTotal],['Sites publicados',metrics.projectsPublished],['Ativações ativas',metrics.activationsActive],['Oportunidades',metrics.opportunities],['Conversões',metrics.conversions],['Valor marcado como convertido',new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(metrics.confirmedValue||0)]];return<div><h1 className="text-3xl font-extrabold">Visão geral</h1><p className="mt-2 text-[#6f6e77]">Saúde e resultado da plataforma, sem tratar valor confirmado como receita processada.</p><div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([label,value])=><div key={String(label)} className="rounded-2xl border bg-white p-5"><p className="text-sm text-[#74727c]">{label}</p><strong className="mt-2 block text-2xl">{value}</strong></div>)}</div><section className="mt-8 rounded-2xl border bg-white p-6"><h2 className="text-xl font-extrabold">Funil da Virou</h2><div className="mt-5 grid gap-3 sm:grid-cols-3 lg:grid-cols-6">{Object.entries(funnel||{}).map(([key,value])=><div key={key}><strong className="text-xl">{String(value)}</strong><p className="mt-1 break-words text-xs text-[#77747f]">{key}</p></div>)}</div></section></div>}
