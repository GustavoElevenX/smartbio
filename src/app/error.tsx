"use client";
import { AlertTriangle, RefreshCw } from "lucide-react";
export default function GlobalError({ error, reset }: { error: Error & { digest?: string; requestId?: string }; reset: () => void }) { const reference = error.requestId || error.digest; return <main className="grid min-h-screen place-items-center bg-[#f7f8fa] p-6 text-center"><div><span className="mx-auto grid size-14 place-items-center rounded-[18px] bg-[#fff0ed] text-[#cc5444]"><AlertTriangle /></span><h1 className="mt-6 text-3xl font-extrabold tracking-[-.04em]">Algo saiu do caminho.</h1><p className="mt-3 text-sm text-[#74747e]">Suas alterações salvas continuam seguras.</p>{reference ? <p className="mt-2 text-xs text-[#74747e]">Código para suporte: {reference}</p> : null}<button onClick={reset} className="mt-7 inline-flex min-h-11 items-center gap-2 rounded-xl bg-[#0054fc] px-5 text-sm font-bold text-white"><RefreshCw size={16} /> Tentar novamente</button></div></main>; }
